services:
  web-communicator-api:
    container_name: web-communicator-api
    restart: always
    build:
      context: ./server/
      dockerfile: Dockerfile
    ports:
      - 8000:8000
  web-communicator-client:
    container_name: web-communicator-client
    restart: always
    build:
      context: ./client/
      dockerfile: Dockerfile
    ports:
     - 3000:3000